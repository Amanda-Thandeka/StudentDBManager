# StudentDBManager
StudentDBManager is a Java class that uses JDBC and the DAO pattern to manage student records in a database. It supports basic CRUD operations on the StudentTBL table and is mainly intended for learning JDBC and simple database-driven Java applications.
